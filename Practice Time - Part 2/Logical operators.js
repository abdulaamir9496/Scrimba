let dayOfMonth = 13;  //if the day of the month is not 13 it won't return the output.
let weekday = "Friday";  //if it's any other day of the week it won't return output.

// If it is Friday the 13th, log out this spooky face: 😱
// Use the logical "AND operator" -> &&

if(dayOfMonth === 13 && weekday === "Friday") {
    // console.log("😊");
}